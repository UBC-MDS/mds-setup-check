---
title: "MDS setup check: R Markdown"
author: "UBC Master of Data Science"
output:
  pdf_document:
    latex_engine: xelatex
---

## What this file checks

Knitting this document to PDF exercises R, `rmarkdown`, `knitr`, pandoc and
LaTeX together.

Note the `latex_engine: xelatex` in the header above. R Markdown uses pdflatex
by default, which cannot typeset the characters at the bottom of this page.

```{r}
R.version$version.string
```

```{r}
data.frame(
  course = c("DSCI 511", "DSCI 521", "DSCI 523"),
  block = c(1, 1, 2),
  language = c("Python", "Python", "R")
)
```

## An image

Assignments include plots and images, which have to survive the render too.

![The UBC MDS logo](mds-logo.png){width=200}

## Mathematics

Inline: the estimator $\hat{\beta} = (X^{\top}X)^{-1}X^{\top}y$ is unbiased.

A display equation:

$$ \mathrm{RSS} = \sum_{i=1}^{n} (y_i - \hat{y}_i)^2 $$

A numbered equation:

\begin{equation}
\hat{\sigma}^2 = \frac{1}{n-p}\sum_{i=1}^{n} e_i^2
\end{equation}

Several lines aligned on the equals sign:

\begin{align}
\mathbb{E}[\hat{\beta}] &= \beta \\
\mathrm{Var}(\hat{\beta}) &= \sigma^2 (X^{\top}X)^{-1}
\end{align}

## Characters that are not plain English

Assignments regularly contain accents, Greek letters, degree signs and proper
dashes. If the line below appears correctly in the PDF, your LaTeX installation
can typeset them:

Montréal · naïve · Öl · 5 °C · α β γ · 10 – 20 · “curly quotes” · ✅ ❌ 📊 ⚠️
